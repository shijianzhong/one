# 改造优化方案

## 当前源码评估（2026-06-04）

该方案的核心方向是合理的，并且当前源码已经大体按这个方向推进：

- `MainAgent` 已替代旧 `Coordinator/GeneralAgent`，成为主对话 Agent。
- `MainAgent` 已暴露 `run_claude_code`、`run_system_task`、`remember`、`recall`、`update_soul` 等工具。
- `Orchestrator` 已负责拦截 `run_claude_code` 和 `run_system_task`，并把 Claude Code 的流式事件透传给 UI。
- 旧 `coordinator.rs`、`general_agent.rs` 已不存在。

需要修正的点：

- `Orchestrator` 源码不应再使用 `coordinator` 字段名或 delegate 注释，应统一为 `main_agent` 语义。
- `run_claude_code` 是流式长任务入口，命令预览必须和真实参数一致，不能在非 `Bypass` 模式展示 `--dangerously-skip-permissions`。
- `JobManager` 当前只是抽出了状态结构，方法仍挂在 `impl AppState` 上；不能把它视为完成解耦。
- `Default` 权限模式目前不是完整“询问/提示”，后续还需要接 UI 确认流程。

以下保留原方案内容，作为架构目标说明。

---

用户消息
    │
    ▼
MainAgent（主 Agent，有人格，持续对话）
    ├── 直接回答 → 流式输出给用户
    └── 工具调用
            ├── run_claude_code(instruction)
            │       └── ClaudeCodeAgent 流式执行
            ├── run_system_task(task)
            │       └── SystemAgent 执行
            ├── remember(fact)
            │       └── MemoryAgent 写入
            └── recall(query)
                    └── MemoryAgent 读取

Orchestrator 变成 MainAgent 的执行壳，不再需要 Coordinator 这个中间层。MainAgent 本身就是那个"主脑"，它既能直接说话，也能调工具。
这和 Claude、ChatGPT 的实际架构一致——一个主模型，配一组工具，工具里可以包含"调用专项子系统"。

对应到代码的改造
要改的核心是 Coordinator，把它升级为 MainAgent：
// 现在的 Coordinator：只有 delegate 工具，只会分派
pub struct Coordinator {
    tools: vec![Arc::new(DelegateTool::new())],  // 只有这一个工具
}

// 改为 MainAgent：有直接回答能力 + 一组真实工具
pub struct MainAgent {
    tools: vec![
        Arc::new(RunClaudeCodeTool::new()),    // 触发 Claude Code 流式
        Arc::new(RunSystemTaskTool::new()),    // 触发 SystemAgent
        Arc::new(RememberTool::new()),         // 写记忆
        Arc::new(RecallTool::new()),           // 读记忆
    ],
    // system_prompt 描述 AI 助手的人格，而不是"协调者"
}

Orchestrator 只需要微调：把 coordinator.step() 改为 main_agent.step()，工具拦截从 delegate 改为拦截具体工具名：

// 现在
if call.name == "delegate" {
    let agent_id = args["agent_id"]...
    run_sub_agent(sub_agents[agent_id], task)
}

// 改后
match call.name.as_str() {
    "run_claude_code" => run_claude_code_stream(args["instruction"]),
    "run_system_task" => run_system_agent(args["task"]),
    "remember"        => memory_agent.save(args["fact"]),
    "recall"          => memory_agent.query(args["query"]),
}

GeneralAgent 可以删除——它的职责（直接回答用户）现在由 MainAgent 自己承担。

总结
现在改后主对话角色缺失（Coordinator 不能直接对话）MainAgent（有人格，能直接回答）工具调用方式delegate → 找 sub-agent直接工具：run_claude_code / run_system / rememberGeneralAgent充当 Coordinator 的"嘴"删除，MainAgent 自己说话Coordinator规划者但能力受限升级为 MainAgentOrchestrator驱动 Coordinator 循环驱动 MainAgent 循环，工具拦截逻辑简化

核心思路
MainAgent 本身不写代码，它只做决策——判断"这个任务需要用 Claude Code 来做"，然后调用 run_claude_code 工具，把任务交出去。
就像一个技术 PM：自己不写代码，但知道什么时候该叫工程师，叫的时候给出清晰的需求描述。

工具设计

// MainAgent 的工具之一
pub struct RunClaudeCodeTool;

impl Tool for RunClaudeCodeTool {
    fn name(&self) -> &str { "run_claude_code" }
    
    fn description(&self) -> &str {
        "当任务需要编写、修改、调试代码，或者操作文件系统时使用。
         Claude Code 会在项目目录里自主完成编码任务并返回结果。"
    }
    
    fn parameters_schema(&self) -> Value {
        json!({
            "type": "object",
            "properties": {
                "instruction": {
                    "type": "string",
                    "description": "给 Claude Code 的完整任务描述，要清晰具体"
                }
            },
            "required": ["instruction"]
        })
    }
}

MainAgent 收到"帮我写一个登录页面"，LLM 判断这是编码任务，返回：

{
  "tool_calls": [{
    "name": "run_claude_code",
    "arguments": {
      "instruction": "创建一个用户登录页面，包含用户名、密码输入框和提交按钮，使用 HTML + CSS，样式简洁现代"
    }
  }]
}

Orchestrator 拦截这个工具调用
这是关键——run_claude_code 不是普通工具（调一下返回结果），它是流式长时任务，需要特殊处理：

// orchestrator.rs
match call.name.as_str() {
    "run_claude_code" => {
        let instruction = args["instruction"].as_str().unwrap_or_default();
        
        // 不走普通 tool.call()，而是走流式路径
        // 通过 on_event 把 ClaudeStreamEvent 实时透传给 UI
        self.run_claude_code_stream(
            instruction,
            session_id,
            on_event,  // ← 每个 delta 都发到外层
        ).await?
    }
    
    "run_system_task" => { ... }
    "remember"        => { ... }
    "recall"          => { ... }
    
    // 其他工具走普通 tool.call() 路径
    _ => tool.call(args).await?.to_string()
}
这样 Orchestrator 对"普通工具"和"流式工具"做了区分处理，ClaudeCode 的实时输出能透传到 UI。

完整的一次交互流程:
用户："帮我写一个登录页面"
    │
    ▼
MainAgent.step(context)
    │  LLM 判断：这是编码任务
    │
    ▼
AgentResponse::ToolCalls([
    { name: "run_claude_code", args: { instruction: "创建登录页面..." } }
])
    │
    ▼
Orchestrator 拦截 run_claude_code
    │  识别为流式任务，特殊处理
    │
    ▼
execute_instruction_stream("创建登录页面...")
    │  实时发送 ClaudeStreamEvent
    │  ├── Progress: "正在创建 login.html..."
    │  ├── AssistantText: "我来创建一个简洁的登录页面..."
    │  └── Finished: "已完成 login.html"
    │
    ▼  把结果塞回 context
tool_result = "已完成，创建了 login.html 和 style.css"
    │
    ▼
MainAgent.step(context)  ← 第二轮，看到工具结果
    │  LLM 生成最终回复
    │
    ▼
AgentResponse::Answer("登录页面已创建完成！包含...")
    │
    ▼
流式输出给用户


关键设计原则
MainAgent 的 instruction 质量决定 Claude Code 的输出质量。
MainAgent 不应该把原始用户消息直接丢给 Claude Code，而是要先理解、再提炼：
用户说："帮我写个登录"（模糊）
    ↓
MainAgent 结合上下文（知道项目用 React、之前讨论过用 Tailwind）
    ↓
生成清晰指令："在 src/pages/ 下创建 Login.tsx，使用 React + Tailwind，
               包含 email/password 表单，接入现有的 useAuth hook"
    ↓
Claude Code 收到精准指令，产出质量高

这是 MainAgent 存在的核心价值：上下文感知 + 指令精炼，而不只是透传。

